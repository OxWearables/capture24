# @package _global_
defaults:
  - /data: default
  - /model: default
  - /optim: default
  - /augment: default
  - /dataloader: default

name: default
n_epochs: 100
early_stop_patience: 10

limit_train_batches: 1.0
limit_val_batches: 1.0
limit_test_batches: 1.0